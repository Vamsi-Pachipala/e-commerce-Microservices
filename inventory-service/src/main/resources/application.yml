

spring:
  application:
    name: inventory-service
  datasource:
    url: dbc:mysql://localhost:3306/inventory_db?createTableIfNotExists=true
    username: root
    password: Vamsi@243
  jpa:
    hibernate:
      ddl-auto: update

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
    register-with-eureka: true
    fetch-registry: true

server:
  port: 0